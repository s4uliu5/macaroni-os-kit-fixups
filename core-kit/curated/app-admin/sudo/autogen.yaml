sudo:
  generator: github-1
  packages:
    - sudo:
        github:
          user: sudo-project
          query: releases
          transform:
            - kind: string
              match: p
              replace: .
