zfs_and_friends:
  generator: github-1
  defaults:
    kernel_compat: "6.12"
    github:
      user: openzfs
      repo: zfs
      query: releases
      match: zfs-([0-9]\.[0-9]\.[0.9])
  packages:
    - zfs
    - zfs-kmod

